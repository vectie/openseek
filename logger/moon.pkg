import {
  "moonbitlang/async/stdio",
  "moonbitlang/core/json",
}

import {
  "moonbitlang/async",
  "moonbitlang/async/stdio",
} for "test"

warnings = "+unnecessary_annotation"

supported_targets = "+native"
