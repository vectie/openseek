// The desktop app's version, in one place.

///|
/// The version packagers stamp into bundle metadata (Info.plist and
/// friends) and the update checker compares against the hosted release
/// manifest. Bump on release.
pub const AppVersion : String = "0.1.4"
